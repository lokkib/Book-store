export type CartItemProps = {
    id: string,
    imageUrl: string,
    title: string,
    type?: string,
    size?: number,
    price: number,
    category: number,
    rating: number,
    count: number

}